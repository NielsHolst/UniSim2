Simulation sim( steps = 30 ) {
  Population pop1(
    bufferSize = 5
    )
  OutputR {
    PageR {
      PlotR (
        ports = (pop1[cohorts] ./total[value])
        )
      {
        Box total (
          &value{transform = Sum} = pop1[cohorts]
          )
      }
    }
  }
}
